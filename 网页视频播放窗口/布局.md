明白！简化设计：**主控制面板本身可拖拽**，开关按钮只做显示/隐藏。让我先看当前代码状态：
用户想要简化设计：
1. 主控制面板（视频悬浮控制面板）本身可以拖拽
2. 不需要单独的开关按钮来控制位置
3. 开关按钮只负责显示/隐藏控制面板

让我先看看当前的实现，理解用户的需求：
- 之前的设计是：开关按钮可拖拽 → 主面板跟随按钮
- 用户现在想要：主面板本身可拖拽，开关按钮只是显示/隐藏开关

这样更简单直接。我需要：
1. 给主控制面板添加拖拽功能
2. 开关按钮恢复为简单的 toggle（点击显示/隐藏）
3. 主面板位置保存到 GM_setValue

让我先读取当前文件的相关部分。

Now I understand the full picture. The user wants to simplify:

1. The **toggle button** should just be a simple show/hide toggle (click to show/hide the main panel)
2. The **main controller panel itself** should be draggable
3. No need for the separate position panel with direction buttons

Let me rewrite:
1. `createToggleButton` - simple click to toggle, no drag
2. `createController` - make it draggable, save position to GM_setValue
3. Remove `showPositionPanel`, `hidePositionPanel`, `adjustControllerOffset`, `resetControllerPosition`, `updateControllerPosition`

Let me do this step by step.

First, update state variables (remove unused ones). Then rewrite createToggleButton to be simple. Then rewrite createController to be draggable. Then remove the position panel functions.

Let me plan the changes:

1. State vars: Remove `toggleDragState`, `positionPanel`, `controllerPosOffset`. Add `controllerPos` for saving controller position.

2. `createToggleButton`: Simple click to toggle (like original). Save position if dragged... wait, user said "不需要开关按钮加控制" meaning the toggle button doesn't need drag control. So it's just a simple toggle.

3. `createController`: Make the panel draggable. When drag ends, save position to GM_setValue. On creation, restore from saved position.

4. Remove: `updateControllerPosition`, `showPositionPanel`, `hidePositionPanel`, `adjustControllerOffset`, `resetControllerPosition`.

Let me read the full createController function to understand its structure, especially where to add the drag handle.

现在清楚了。简化设计：**主面板本身可拖拽**，开关按钮只做显示/隐藏。开始改造：

**第一步：替换状态变量**
Now I have a clear picture. Let me make all the changes:

1. Replace state variables
2. Replace `createToggleButton` with simple toggle
3. Add drag functionality to `createController`
4. Remove position panel functions
5. Update `init`, `toggleController`, `destroy`, `controllerObserver`

Let me do this step by step. First, the state variables.

**第二步：修改 createController，添加拖拽手柄和保存位置**
Good. Now let me read the createController function more fully to understand the layout, then add drag handle to it.

Let me find where the controller is appended to the body to understand the full structure.

Let me look at where the controller is appended (around line 398) and understand the full createController function end.

现在查看 createController 开头，添加拖拽手柄和位置恢复：

现在重写 createController，添加拖拽手柄 + 位置保存：
Now I need to:

1. Add drag handle to the controller (a header bar with grip)
2. Add drag functionality
3. Save position on drag end
4. Restore position on creation

Let me modify the createController function. I'll add a drag handle at the top and wrap the contents below it.

The plan:
- Add a header bar as first child with grip icon (⋮⋮ or ⋯)
- The header bar acts as drag handle
- Load saved position or use default
- After appending to body, set initial position
- Add drag handlers

Let me now make the changes. First, let me modify the controller's cssText to remove transition (which conflicts with drag) and add cursor for the handle. Then add the handle element and drag logic.

Let me replace the controller creation section.

**第三步：修改 append 结构，把内容放到 controllerInner 里**
Now I need to change where the controller content is appended. Previously it was `controller.append(controlWrap, progressWrap)` and `document.body.appendChild(controller)`. Now I need to:

1. Append controlWrap and progressWrap to `controllerInner` instead of `controller`
2. Append dragHandle and controllerInner to `controller`
3. Append controller to body

Let me find those lines and update them.

**第四步：简化 createToggleButton，恢复为纯开关按钮**
Now I need to replace the `createToggleButton` function - simplify it to just toggle controller visibility on click (no dragging).

**第五步：更新 toggleController 和 init**
Now I need to update `toggleController` and `init` functions, and `destroy` function. Let me find them.

**第六步：更新 init 函数**
Now let me find and update the init function.

**第七步：更新 destroy 函数**
Now update the destroy function. Let me find it.

现在检查是否有残留的旧引用：
Now let me also handle the reset of controller position. I should add a "reset position" feature somewhere, maybe by double-clicking the toggle button or through some other mechanism. But the user said "不需要开关按钮加控制" so maybe they just want to keep it simple without any reset. Let me add a double-click on the toggle button to reset the controller position though, that would be useful.

Actually let me re-read the user's request: "主控制面板本身可拖拽，开关按钮只做显示/隐藏". That's what I've done. The user didn't ask for a reset feature.

But I should handle the case where the user wants to reset the position. Let me add a double-click on the toggle button to reset.

Let me also make sure the CSS transition for the controller doesn't interfere with dragging. The current `transition: opacity 0.3s ease` should be fine since it only transitions opacity.

Let me also check if there are any references to `updateControllerPosition`, `showPositionPanel`, `hidePositionPanel`, `toggleDragState`, `positionPanel`, `controllerPosOffset` remaining in the code that need cleanup.

没有残留。现在添加一个**双击开关按钮重置位置**的实用功能：
All old references are cleaned up. Now let me add a double-click feature on the toggle button to reset the controller position to default. That's a useful feature.

Also let me verify the overall structure is good by checking the flow once more.

Actually let me add a reset feature - double click the toggle button to reset controller position.

等等，`ondblclick` 和 `onclick` 会冲突（双击会触发两次 click）。改用长按重置更可靠：
Wait, `ondblclick` and `onclick` can conflict - when you double-click, it fires two single clicks first, then the double-click. This means `toggleController` would be called twice (net effect is toggling on then off, or off then on), and then the reset happens. This is a problem.

I should change the approach. Maybe use a delay to distinguish single click from double click. Or use a different approach - like a long press for reset. Or just remove the reset and let users manually clear GM_setValue.

Actually, a simpler approach: the toggle button can have a long-press (500ms) to reset position instead of double-click. That avoids the click/dblclick conflict.

Let me fix this.

验证一下关键区域是否完整：
Let me verify the final file to make sure everything is coherent. Let me check the key sections:

1. State variables - ✓
2. createController with drag handle - ✓
3. createToggleButton with simple toggle + long press reset - ✓
4. toggleController - ✓ (no more updateControllerPosition)
5. init - ✓
6. destroy - ✓

Let me also verify the `createController` function doesn't reference anything that was removed. Let me check for any remaining issues.

Actually, let me also verify the `refreshDomCache` function still works since it uses `document.getElementById` which should still work.

Let me do a final scan to make sure nothing is broken.

Good. Let me also verify the destroy function looks right.

改造完成！总结一下新的设计：

## 新设计

### 圆形悬浮按钮（右下角）
- **单击** → 显示/隐藏主控制面板
- **长按 600ms** → 重置主面板到默认位置

### 视频悬浮控制面板
- **顶部拖拽手柄（⋮⋮）** → 拖动整个面板到任意位置
- 松开鼠标/手指后自动保存位置到 `GM_setValue`
- 下次打开自动恢复上次位置
- 面板内容区域（按钮、进度条）正常使用不受拖拽影响

### 改动对比
| 之前 | 现在 |
|------|------|
| 开关按钮可拖拽 | 开关按钮纯点击（更稳定） |
| 主面板跟随按钮位置 | 主面板独立拖拽 |
| 位置微调面板（方向键） | 已移除（直接拖更直观） |
| 需要两套定位逻辑 | 只有主面板自身一套定位 |